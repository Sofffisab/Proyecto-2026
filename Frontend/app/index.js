import { registerRootComponent } from 'expo'
import App from './App'

// Punto de entrada de la app.
// registerRootComponent asegura que funcione tanto en Expo Go como en builds nativos.
registerRootComponent(App)
